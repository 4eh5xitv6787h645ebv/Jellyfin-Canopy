// Every Seerr owner is now a manifest-backed lazy feature. Keep this
// compatibility barrel import-pure so disabled and off-route sessions cannot
// pull the Seerr closure into the classic cold graph.
