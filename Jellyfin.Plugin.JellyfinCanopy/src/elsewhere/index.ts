// src/elsewhere/index.ts
// their required execution order (the former allComponentScripts order).
// Owned by the elsewhere conversion wave; main.ts imports this barrel once,
// so conversions never edit main.ts itself.
// Elsewhere and reviews are independently loader-owned detail-route entries.
