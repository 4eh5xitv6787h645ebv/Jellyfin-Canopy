# Risk register

Tracking issue: [#39 — EP-00](https://github.com/4eh5xitv6787h645ebv/Jellyfin-Canopy/issues/39)
Status: **accepted** (EP-00). Reviewed at every milestone exit gate and scope
decision.

Scoring: likelihood and impact are Low / Medium / High. *Exposure* is the
combination. Security risks live in the [threat model](threat-model.md); this
register is about delivery, design and programme risk.

| # | Risk | L | I | Exposure | Mitigation | Owner |
|---|---|---|---|---|---|---|
| R-01 | **Nobody adopts it.** The platform is built and no third party ever writes an extension, so the whole programme is cost without return. | High | High | **High** | The completed pilot dogfoods three Canopy families. ADR-0013 permits only the internal server tranche, with a separate-plugin fixture and a new credential-bound Z3b headless service fixture as conformance consumers, and stops before web/public-SDK/GA cost. Three independent external consumers remain an EP-10/EP-11 requirement; internal fixtures do not satisfy it. | programme |
| R-02 | **Native clients never adopt**, so the platform is web-only in practice despite designing a portable protocol. | ~~High~~ **Medium** | Medium | **Medium** | **Materially reduced (2026-07-28):** a first-party Kotlin fork of `jellyfin-androidtv` is in development by the same owner and is a committed consumer, so the protocol has a real adopter and a real design partner rather than a hypothetical one. Keep the native schema deliberately tiny and document the first-party integration; a public multi-client adoption guide remains deferred until the broader programme resumes. Keep saying "unsupported" plainly in the [client matrix](supported-client-matrix.md) for every client that has *not* adopted. Residual risk is now third-party adoption, not adoption at all. | EP-08 |
| R-03 | **Scope explosion.** Twelve milestones with GA-grade acceptance criteria is a multi-year programme for a plugin. | High | High | **High** | ADR-0013 activates only the server prefix C2-C4, the bounded registry/provider C5 subset and corresponding C10 diagnostics. Web, broader native, public SDK, adoption and GA remain deferred. Work advances one bounded child and one owning layer at a time; milestone parents close only on their full exit gates. | programme |
| R-04 | **The web adapter is the real cost.** Partially retired: slot rendering, idempotent mounting, teardown, two-contribution coexistence and frame isolation are measured on the supported **modern layout** ([S17](spike-evidence.md#s17--browser-slots-render-idempotently-the-frame-is-genuinely-isolated-and-there-is-no-csp)). What remains is the modern mobile/WebView matrix, a11y, i18n and the jank budgets. The classic layout is explicitly outside Canopy support. | ~~High~~ **Medium** | High | **High** | The mechanism is proven; the supported breadth is not. EP-07 must run the same spec across every supported modern surface before implementation is called done. | EP-07 |
| R-05 | **Canopy's own velocity drops.** Kernel work competes with features and the bug inventory for the same maintainer. | Medium | High | **High** | Project 3 sequences the active server tranche explicitly and keeps each PR bounded. EP-12 remains last, so unrelated Canopy work never depends on an unfinished platform. | programme |
| R-06 | **Jellyfin 12 changes underneath.** Load-order behaviour, plugin loading, auth or the web client shift in a patch release. | Medium | High | **High** | Depend on no undocumented ordering for authority: #652 binds a single materialized host inventory into a GUID-canonical registry and independently exercises fixtures named on both sides of Canopy. Pin a tested patch matrix and keep the weekly compatibility probe advisory but watched. | EP-11 |
| R-07 | **The kernel stays fused to Canopy.** The host-adapter seam erodes and extraction becomes impossible. | Medium | Medium | **Medium** | The seam is a build-enforced rule: an architecture test asserting the kernel references no `MediaBrowser.*` type directly, in the same style as the existing `AtomicFile` write guard. | EP-01 |
| R-08 | **Duplicate business logic.** A platform adapter grows its own copy instead of calling the owning service. | Medium | High | **High** | Parity tests proving legacy route and platform route reach the same owner. This is already a repository rule; the platform makes violating it much easier. | EP-06 |
| R-09 | **Public contract frozen too early**, locking in a design flaw for the life of v1. | Medium | High | **High** | The internal-facing Platform v1 floor is additive-only and machine-gated. New server-tranche contracts land one bounded slice with conformance fixtures; public support remains withheld until EP-10. Two majors may coexist. | EP-10 |
| R-10 | **`RequestIdentityService` promoted without ladder-wide tests.** There is no dedicated test file; direct coverage exists in `Tests/Services/SpoilerIdentityTests.cs`, which constructs the service and asserts on its ladder — but scoped to the Spoiler Guard path. | Medium | High | **High** | Promotion is blocked until the full ladder, including the session-by-IP and cookie tiers, is covered on its own terms. Confidence tiers stay disambiguation-only. | EP-02 |
| R-11 | ~~**The two caller-id resolvers diverge further.**~~ Authority-relevant callers could previously use parsers with different claim fallbacks. | Low | High | **Medium** | **Mitigated by #638:** one authenticated, claims-only resolver now owns Jellyfin user-id parsing; explicit cross-user targeting first requires that canonical actor; mixed-role controller logic cannot treat a role claim as administrator authority without it; Platform preserves its stricter same-identity API-key and fresh-host-user checks. Behavioral regressions cover session control and cross-user mutation, while a production-wide source guard forbids legacy fallbacks and confines `RequestIdentityService` to non-authoritative Spoiler disambiguation/topology wiring. EP-02 must retain these gates as actor kinds expand. | EP-02 |
| R-12 | **Coverage and warning ratchets block delivery**, tempting someone to weaken a floor. | Medium | Medium | **Medium** | Never weaken a ratchet. Platform code ships with its tests in the same PR. A tolerance requires repeated identical-scope measurements, a written rationale and a negative boundary test. | every milestone |
| R-13 | **Performance regression.** Reflection, JSON round-trips, schema validation and provider fan-out add latency to hot paths. | Medium | Medium | **Medium** | Publish budgets before implementing. Batch and lazily resolve contributions. Measure platform overhead against the direct path in EP-10. Keep the nightly scale tier in view. | EP-10 |
| R-14 | **Silent failures reach production.** The DI-returns-`null` mode is the archetype. | Medium | High | **High** | Every failure maps to a documented machine code; health probes and an explicit `incompatible` state; a "no silent failure" success metric that is actually tested. | EP-04 |
| R-15 | ~~A hot-disable assumption proves false.~~ **Confirmed false, and worse than assumed.** Disable yields `PluginStatus.Restart`, not `Disabled`; enable does not undo it without a restart; a runtime drop-in install is not discovered; and the load context is **never unloaded**, so an uninstalled extension's code stays in the process. | — | High | **Resolved into design** | The kernel enforces lifecycle state at invocation rather than trusting the host, `restart-pending` is a first-class state, and no document claims revocation stops code from existing. Measured in [S15](spike-evidence.md#s15--hot-lifecycle-nothing-is-ever-unloaded-and-disable-needs-a-restart). | closed by #493 |
| R-16 | **Load and concurrency behaviour unknown.** Every spike probe was sequential. | High | Medium | **High** | The native pilot proves bounded request/action concurrency only. Active EP-04/EP-05 own provider fan-out, event storms and backpressure budgets; EP-11 retains multi-hour soak. Pilot evidence must not be cited for those claims. | EP-04, EP-05 |
| R-17 | **Documentation drifts from the contract**, so consumers build against prose. | Medium | Medium | **Medium** | Checked-in OpenAPI/JSON Schema as the source of truth; CI rejects drift, stale examples and unverified snippets. This is the specific failure the current 183-route surface represents. | EP-09 |
| R-18 | **Two extension systems emerge** — the platform plus a resurrected user-scripts escape hatch. | Low | High | **Medium** | [Milestone 82 is superseded](milestone-82-disposition.md), with the reasoning recorded so it is not re-litigated. EP-11 closes it explicitly. | EP-11 |
| R-19 | **Registry state corruption or stale reconciliation** loses grants, approvals or publishes obsolete host facts. | Low | High | **Medium** | #650 reuses `AtomicFile`, rejects the whole strict bounded state, hydrates authority dormant, and requires a fresh elevated recovery decision into one of eight immutable evidence-preserving epochs. #652 adds opaque one-use reconciliation epochs: every accepted trigger fences first; stale, duplicate or foreign completion is rejected before persistence; cancellation abandonment is linearized against whole commit; and stop or failure stays fenced until a later whole commit. Interrupted/repeated recovery, exact-bound, concurrency and corrupt-state tests fail closed; epoch exhaustion requires operator repair. | EP-03 |
| R-20 | **Rebranding drift.** Roadmap issues say `JellyfinElevate`; the code says `JellyfinCanopy`. A contract could ship with the wrong identifier. | Medium | Medium | **Medium** | Corrected once, prominently, in [charter §0](charter.md#0-naming-before-anything-else). Child issues use the real identifiers. | EP-01 |
| R-21 | **Provider responses are unbounded.** No response cap exists today; a 2 MB response passed through the spike untouched. | Medium | Medium | **Medium** | A response cap is an EP-04 acceptance criterion, recorded in [ADR-0004](adr/0004-provider-invocation.md) as new work rather than an existing property. | EP-04 |
| R-22 | **A namesake plugin can delete the Canopy install.** Jellyfin's old-version cleanup deduplicates by manifest `name` alone and deletes the loser's directory. Reachable by accident, not only by malice. | Low | High | **Medium** | Cannot be prevented from a plugin. Document the exact `name` as reserved, add a startup self-check for the expected plugin root, and avoid introducing a second reserved name — see **T-16** and [charter §6](charter.md#6-kernel-placement-decision). | EP-03 |

## Risks accepted without mitigation

| Risk | Why accepted |
|---|---|
| A malicious installed plugin can do anything a plugin can do | Not containable. Installing a plugin is an administrator trust decision made before the platform is involved. Documented as **T-03**; no programme document may claim otherwise. |
| A runaway provider cannot be terminated | .NET offers no mechanism. The deadline protects the caller only. |
| Jellyfin's permissive CORS | The host's behaviour, not ours. Every request is authorized on its own merits instead. |
